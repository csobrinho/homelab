# From https://github.com/cilium/cilium/blob/main/install/kubernetes/cilium/values.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - bgp-config.yaml
  - lb-pools.yaml

helmCharts:
  - name: cilium
    repo: https://helm.cilium.io/
    releaseName: cilium
    namespace: kube-system
    version: 1.18.2
    includeCRDs: true
    # Based on: https://github.com/onedr0p/home-ops/blob/main/kubernetes/apps/kube-system/cilium/app/helmrelease.yaml
    valuesInline:
      policyEnforcementMode: never # Disable for now.
      autoDirectNodeRoutes: true
      bandwidthManager:
        enabled: false
        bbr: ~
      egressGateway:
        enabled: true
      ipv6:
        enabled: false
      bpf:
        masquerade: true
        preallocateMaps: true
        tproxy: true
        hostLegacyRouting: true
        lbExternalClusterIP: true
      bpfClockProbe: true
      bgpControlPlane:
        enabled: true
      dashboards:
        enabled: true
      endpointRoutes:
        enabled: true
      envoy:
        enabled: false
      hubble:
        enabled: true
      ipam:
        mode: kubernetes
      ipv4NativeRoutingCIDR: 10.42.0.0/16
      k8s:
        apiServerURLs: "https://10.10.1.11:6443 https://10.10.1.12:6443 https://10.10.1.13:6443"
      kubeProxyReplacement: "true"
      l2announcements:
        enabled: false
      loadBalancer:
        algorithm: maglev
        mode: dsr
      localRedirectPolicy: true
      operator:
        dashboards:
          enabled: true
        prometheus:
          enabled: true
          serviceMonitor:
            enabled: true
        replicas: 2
        rollOutPods: true
      pmtuDiscovery:
        enabled: true
      prometheus:
        enabled: true
        serviceMonitor:
          enabled: true
          trustCRDsExist: true
      rollOutCiliumPods: true
      routingMode: native
      securityContext:
        capabilities:
          ciliumAgent:
            - CHOWN
            - KILL
            - NET_ADMIN
            - NET_RAW
            - IPC_LOCK
            - SYS_ADMIN
            - SYS_RESOURCE
            - PERFMON
            - BPF
            - DAC_OVERRIDE
            - FOWNER
            - SETGID
            - SETUID
          cleanCiliumState:
            - NET_ADMIN
            - SYS_ADMIN
            - SYS_RESOURCE
