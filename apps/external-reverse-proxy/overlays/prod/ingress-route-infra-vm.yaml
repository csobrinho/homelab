kind: IngressRoute
apiVersion: traefik.io/v1alpha1
metadata:
  name: infra-vm-ingress
  namespace: infra
  annotations:
    external-dns.alpha.kubernetes.io/target: infra-int
spec:
  entryPoints:
    - websecure
    - websecure-ext
  routes:
    - match: Host(`infra-vm.sobrinho.pt`)
      kind: Rule
      services:
        - name: infra-vm
          port: 8006
          scheme: https
          serversTransport: traefik-transport-insecure@kubernetescrd
  tls:
    secretName: infra-vm-cert
---
kind: Certificate
apiVersion: cert-manager.io/v1
metadata:
  name: infra-vm-cert
  namespace: infra
spec:
  secretName: infra-vm-cert
  dnsNames:
    - infra-vm.sobrinho.pt
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
