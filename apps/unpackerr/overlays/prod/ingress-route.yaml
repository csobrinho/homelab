kind: IngressRoute
apiVersion: traefik.io/v1alpha1
metadata:
  name: unpackerr-ingress
  namespace: media

spec:
  entryPoints:
    - websecure
    - websecure-ext
  routes:
    - match: Host(`unpackerr.sobrinho.pt`)
      kind: Rule
      services:
        - name: unpackerr
          port: main
