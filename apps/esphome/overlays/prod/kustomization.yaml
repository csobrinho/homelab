# From https://github.com/trueforge-org/truecharts/blob/master/charts/library/common/values.yaml
# From https://github.com/trueforge-org/truecharts/blob/master/charts/stable/esphome/values.yaml
# yaml-language-server: $schema=https://www.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress-route.yaml

helmCharts:
  - name: esphome
    repo: oci://oci.trueforge.org/truecharts
    releaseName: esphome
    namespace: esphome
    version: 25.8.0
    includeCRDs: true
    valuesInline:
      TZ: America/Los_Angeles
      podOptions:
        nodeSelector:
          kubernetes.io/arch: "arm64"
        hostNetwork: true
      persistence:
        config:
          enabled: true
          retain: true
          mountPath: /config
          storageClass: "ceph-block"
          accessMode: ReadWriteOnce
          size: 256Mi
        platformio:
          enabled: true
          retain: true
          mountPath: /root/.platformio
          storageClass: "ceph-block"
          accessMode: ReadWriteOnce
          size: 8Gi
        cache:
          enabled: true
          retain: true
          storageClass: "ceph-block"
          accessMode: ReadWriteOnce
          size: 24Gi
        build:
          enabled: true
          type: emptyDir
          size: 12Gi
        dev:
          enabled: true
          type: nfs
          path: /data/dev/esphome/components
          server: infra1
          mountPath: /development/components
      resources:
        limits:
          cpu: 750m
          memory: 1Gi
        requests:
          cpu: 500m
          memory: 200Mi

components:
  - ../../../../components/managed-sablier
