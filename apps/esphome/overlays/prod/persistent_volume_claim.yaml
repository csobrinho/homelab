kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config-pvc
  namespace: esphome

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 64Mi
