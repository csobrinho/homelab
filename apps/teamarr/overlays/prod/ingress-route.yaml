kind: IngressRoute
apiVersion: traefik.io/v1alpha1
metadata:
  name: teamarr-ingress
  namespace: media
  annotations:
    external-dns.alpha.kubernetes.io/target: infra-int
spec:
  entryPoints:
    - websecure
    - websecure-ext
  routes:
    - match: Host(`teamarr.sobrinho.pt`)
      kind: Rule
      services:
        - name: teamarr
          port: main
  tls:
    secretName: teamarr-cert
---
kind: Certificate
apiVersion: cert-manager.io/v1
metadata:
  name: teamarr-cert
  namespace: media
spec:
  secretName: teamarr-cert
  dnsNames:
    - teamarr.sobrinho.pt
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
