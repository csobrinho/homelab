apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - middleware-sablier.yaml

patches:
  - target:
      group: apps
      kind: Deployment
    patch: |-
      - op: remove
        path: /spec/replicas
