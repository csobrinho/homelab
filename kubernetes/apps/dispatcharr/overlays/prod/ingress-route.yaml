kind: IngressRoute
apiVersion: traefik.io/v1alpha1
metadata:
  name: dispatcharr-ingress
  namespace: media
  annotations:
    external-dns.alpha.kubernetes.io/target: infra-int
spec:
  entryPoints:
    - websecure
    - websecure-ext
  routes:
    - match: Host(`dispatcharr.sobrinho.pt`)
      kind: Rule
      services:
        - name: dispatcharr
          port: main
  tls:
    secretName: dispatcharr-cert
---
kind: Certificate
apiVersion: cert-manager.io/v1
metadata:
  name: dispatcharr-cert
  namespace: media
spec:
  secretName: dispatcharr-cert
  dnsNames:
    - dispatcharr.sobrinho.pt
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
