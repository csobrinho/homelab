apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - middleware.yaml

patches:
  - target:
      group: apps
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/replicas
        value: 0
  - target:
      group: apps
      kind: StatefulSet
    patch: |-
      - op: add
        path: /spec/replicas
        value: 0

transformers:
  - transformer-labels.yaml
